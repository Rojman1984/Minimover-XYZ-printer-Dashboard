# Copy to /etc/default/mjpg-streamer and edit as needed
# Example variables used by the systemd unit

# The -i and -o args for mjpg_streamer
# Input: input_uvc.so (UVC camera)
# Output: output_http.so (HTTP server)
# Adjust device (-d), resolution (-r), fps (-f) and quality (-q) as required.

MJPG_INPUT_OPTS='-i "input_uvc.so -d /dev/video0 -r 640x480 -f 30 -q 85"'
MJPG_OUTPUT_OPTS='-o "output_http.so -w /usr/local/share/mjpg-streamer/www -p 8080"'

# Combined args (used by ExecStart in unit)
MJPG_ARGS="$MJPG_INPUT_OPTS $MJPG_OUTPUT_OPTS"